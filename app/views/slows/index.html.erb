<div class="w-full px-8 py-6 bg-white shadow-md rounded-lg">
  <h1 class="font-bold text-4xl mb-6">Pokes</h1>

  <div class="space-y-4">
    <% @pokes.each do |name, poke| %>
      <div class="p-4 bg-gray-100 rounded-lg">
        <h2 class="font-bold text-2xl mb-2"> <%= link_to name, slow_path(id: name) %> </h2>
        <!-- Display other poke details here -->
      </div>
    <% end %>
  </div>
</div>